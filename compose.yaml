services:
  api:
    build: ./apps/api
    environment:
      WEAVANCE_ENVIRONMENT: local
    ports:
      - "8000:8000"

  web:
    build:
      context: ./apps/web
      args:
        VITE_API_URL: http://localhost:8000
    depends_on:
      - api
    ports:
      - "5173:80"
